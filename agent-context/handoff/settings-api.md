# Settings API

Client-only settings (theme). No backend endpoints.

## Implemented API

_Not applicable — no HTTP API. Theme preference is stored in `localStorage` (`engapp-theme`)._

## Expected API

_Not applicable._

## Notes

- Auth: none (theme available to guests and signed-in users)
- Persistence: browser `localStorage` only; no server sync
